# CartNest

A static online shopping cart storefront built with semantic HTML and Bootstrap CSS. It uses no JavaScript.

Open `index.html` directly in a browser. Product imagery is loaded from Unsplash and Bootstrap assets are loaded from jsDelivr.
